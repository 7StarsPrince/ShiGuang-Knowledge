# 拾光知识库 Python 依赖（版本锁定，确保跨平台效果一致）
# 安装命令: pip3 install -r requirements.txt

# --- 通用音频处理 ---
numpy==1.26.4
soundfile==0.13.1
scipy==1.13.1

# --- 音频格式转换 (scripts/convert_to_mp3.py) ---
# 无需 ffmpeg，使用 soundfile 解码 + lameenc 编码 MP3
lameenc>=1.8.0

# --- 人声分离 - noisereduce (scripts/enhance_audio_demucs.py) ---
# 注意: noisereduce 的 stationary=False 模式对 scipy 版本敏感
# 不同版本降噪效果差异很大，必须锁定版本
noisereduce==3.0.3
joblib>=1.3.0
tqdm>=4.65.0
matplotlib>=3.7.0

# --- 人声分离 - DeepFilterNet3 (scripts/enhance_audio.py, 可选) ---
# 首次使用时会自动下载模型，需要联网
deepfilternet==0.5.6
torch>=2.0.0

# --- 语音识别 (scripts/transcribe_whisper.py) ---
# openai-whisper 首次使用时会自动下载模型，需要联网
openai-whisper==20240930
resampy>=0.4.2

# --- PDF 页面渲染 (scripts/render-pdf-pages.py) ---
# 用于扫描版/照片型 PDF 的 OCR：将 PDF 页面转为 PNG 图片后交给多模态 LLM 识别
PyMuPDF>=1.24.0
